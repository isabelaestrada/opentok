<%= form_with(model: visit, local: true) do |form| %>
  <% if visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
      <% visit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_area :location, id: :visit_location %>
  </div>

  <div class="field">
    <%= form.label :geocoder %>
    <%= form.text_area :geocoder, id: :visit_geocoder %>
  </div>

  <div class="field">
    <%= form.label :device %>
    <%= form.text_area :device, id: :visit_device %>
  </div>

  <div class="field">
    <%= form.label :backtrace %>
    <%= form.text_area :backtrace, id: :visit_backtrace %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
